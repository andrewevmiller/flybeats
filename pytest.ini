[pytest]
# Registering the marker keeps `pytest -m gpu` from warning, and makes
# `-m "not gpu"` a supported way to say "only what CI can run".
markers =
    gpu: needs a CUDA device. Skipped on CI and in cloud sessions, which have none.
testpaths = tests

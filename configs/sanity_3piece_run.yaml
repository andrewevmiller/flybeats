# Open question 3, run at a size that finishes on CPU: does the rate-based
# relaxation converge on a 3-piece task before anyone invests in the
# surrogate-gradient spiking version?
_base_: sanity_3piece.yaml
name: sanity_3piece_run
data:
  synthetic: true
  n_clips: 64
  seconds: 2.0
train:
  epochs: 12
  batch_size: 8
  tbptt_steps: 100
  lr: 0.005

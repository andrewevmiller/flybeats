# Core
numpy>=1.26
pandas>=2.0
pyarrow>=14.0
torch>=2.2
scipy>=1.11
pyyaml>=6.0

# Audio / MIDI (Phase 2/3/5)
soundfile>=0.12
pretty_midi>=0.2.10

# Realtime (Phase 5, optional - needs system audio/MIDI libs)
# sounddevice>=0.4
# mido>=1.3
# python-rtmidi>=1.5

# Phase 0 data access (optional: neuPrint API path; the flat-connectome
# feather path in scripts/fetch_data.py needs none of these)
# neuprint-python>=0.6
